// Stub — full implementation pending. Renders nothing visible
// so the project overview page compiles and behaves normally.
export function ProjectClientsPanel({ projectId: _projectId }: { projectId: string }) {
  return null;
}
